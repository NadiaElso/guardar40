let cargaDeProductos:string=String(("Ingrese los nuevos productos"));
let cargaDePrecios:number=Number(("Ingrese los nuevos precios"));
let cargaDeStock:number=Number(("Ingrese Stock de los nuevos productos"));


function cargarListaDeprecios(cargaDeProductos:string[],cargaDePrecios:number[],cargaDeStock:number[]) {
  for (let index = 0; index < cargaDeProductos.length; index++) {
    
    
  }
  for (let index = 0; index < cargaDePrecios.length; index++) {
    }
    for (let index = 0; index < cargaDeStock.length; index++) {
      console.log(cargaDeProductos[index],cargaDePrecios[index],cargaDeStock[index]);
      
    }

}

